# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: grounded-inference-api
metadata:
    template: azd-init@1.12.0
services:
    grounded-inference-api:
        project: .
        host: containerapp
        language: dotnet
        docker:
            path: Dockerfile
            context: ..\..\..
